import { flexbox } from '@mui/system';
import React from 'react';

function Loading(props) {
    return (
        <div style={{ display: "flex", height:"80vh", width:"100%", flex: 1, alignItems:"center", justifyContent:"center" }} >
            Loading
        </div>
    );
}

export default Loading;